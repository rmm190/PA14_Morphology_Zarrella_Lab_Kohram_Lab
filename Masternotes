#Making a box and whisker plot for cell length, cell width, and mean cell fluorescence

```
library(ggplot2)

ggplot(data,
       aes(x = Timepoint,
           y = Length..um.)) +
  geom_boxplot() +
  theme_classic() +
  labs(
    x = "Time Point",
    y = "Cell Length (µm)"
  )
```
